import { AnimatePersonDirective } from './animate-person.directive';

describe('AnimatePersonDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatePersonDirective();
    expect(directive).toBeTruthy();
  });
});
