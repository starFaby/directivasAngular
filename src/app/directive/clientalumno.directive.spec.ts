import { ClientalumnoDirective } from './clientalumno.directive';

describe('ClientalumnoDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientalumnoDirective();
    expect(directive).toBeTruthy();
  });
});
